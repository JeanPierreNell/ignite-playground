{
  "name": "apache-poc",
  "version": "1.0.0",
  "description": "Apache POC for Tazama",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json ",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jean-Pierre Nell",
  "license": "Apache-2.0",
  "dependencies": {
    "apache-ignite-client": "^1.0.0",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}